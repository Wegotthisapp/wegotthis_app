VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_anon_key

# Only needed for the Capacitor mobile build (leave unset for the web app).
# Base URL of the deployed Vercel app, used to reach /api/generateDescription
# and /api/suggestPrice, and as the Supabase signup email confirmation link.
# Example: https://jagodoo.vercel.app
VITE_API_BASE_URL=
VITE_SITE_URL=